import { getRepository, Repository } from "typeorm";

async function addTokenToBlack(token: string) {
  try {
    
  }
  catch(e) {
    throw e;
  }
}

async function checkBlackList(token: string) {
  try {
    
  }
  catch(e) {
    throw e;
  }
}

export default {
  addTokenToBlack,
  checkBlackList
}